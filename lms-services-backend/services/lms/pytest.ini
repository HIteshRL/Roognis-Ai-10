[pytest]
# Sprint 3, S3.0: this service's tests have only ever run with services/lms
# as rootdir (conftest.py does `from database import Base`, a bare import
# that only resolves when this directory is on sys.path). CLAUDE.md
# previously documented `python -m pytest services/lms/tests` from the repo
# root as well, which does not work — this file settles it. Run tests from
# this directory: `cd services/lms && pytest -q`.
testpaths = tests
